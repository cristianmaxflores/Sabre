import { PlayerComponent as reducer} from './playerList.reducer'
import { playerListActions as actions} from './playerList.actions'
import * as selectors from './playerList.selector'

export default { reducer, actions, selectors}
